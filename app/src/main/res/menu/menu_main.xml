<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:app="http://schemas.android.com/apk/res-auto" xmlns:android="http://schemas.android.com/apk/res/android">
    <item
            android:id="@+id/searchView"
            android:icon="@drawable/ic_search_black_24dp"
            android:title="Buscar contacto" app:actionViewClass="android.widget.SearchView" app:showAsAction="always"/>
    <item
            android:id="@+id/switchView"
            android:title="Cambiar vista"
            app:actionLayout="@layout/switch_item" app:showAsAction="always"/>

    <item
            android:id="@+id/iNuevo"
            android:title="Nuevo contacto"
            android:icon="@drawable/ic_add_black_24dp"
            app:showAsAction="always"/>
</menu>